---
description: "Initialize GRACE framework in the project — create docs/, AGENTS.md, and XML templates"
---

Load the `grace-init` skill and follow its instructions to bootstrap GRACE framework structure for this project.

Steps:
1. Read the `grace-init` SKILL.md from `.agents/skills/grace-init/`
2. Gather project info from the user (name, keywords, language, framework, libraries, testing stack)
3. Create `docs/` directory and populate XML templates from the skill's `assets/` directory
4. Create or verify `AGENTS.md` at project root
5. Print summary and suggest next step: run `/grace:plan`
